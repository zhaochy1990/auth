# Vendored dependencies (regenerate with `go mod vendor` before docker build)
/vendor/

# Binaries
*.exe
/auth-service
/bin/

# Local env / emulator state
.env

# Vendored dependencies are generated before Docker builds.
vendor/

# JWT keys — provide at runtime (mounted volume / Tencent Cloud); never commit
keys/*.pem

# Go debug binaries (dlv)
__debug_bin*
